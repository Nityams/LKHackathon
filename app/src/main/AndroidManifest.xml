<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hackmesenpai.com.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage" />
        <activity android:name=".CreateAuction" />
        <activity android:name=".Profile" />
        <activity
            android:name=".UploadDrawer"
            android:label="@string/title_activity_upload_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SellerAuction" />
        <activity android:name=".BuyerAuction" />
        <activity
            android:name=".GreenFlash"
            android:label="@string/title_activity_green_flash"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RedFlash"></activity>
    </application>

</manifest>